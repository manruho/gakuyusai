# gakuyusai レジ・受取連携機能 追加仕様書

- 文書名: レジ・受取連携機能 追加仕様書
- 対象システム: `gakuyusai`
- 版: v1.0
- 作成日: 2026-07-16
- 位置づけ: 既存の「gakuyusai 仕様書」に追加する実装・運用仕様

> 本書は、4台のレジと4台の受取用PCを1対1で対応させ、会計確定と同時に注文番号と商品内容を受取場所へリアルタイム配信する機能を定義する。

---

## 1. 目的

文化祭の食品販売において、会計場所と商品受取場所を分離し、次の流れを実現する。

1. レジ担当者が商品を選び、会計を確定する。
2. サーバーがランダムな注文番号を発行する。
3. レジ画面に注文番号を大きく表示する。
4. レジ担当者が、レジごとに決められた色紙へ注文番号を手書きし、来場者へ渡す。
5. 注文番号と商品内容を、対応する受取用PCへ即時配信する。
6. 受取担当者が商品を準備し、色紙と注文番号を照合して商品を渡す。
7. 受渡し後、受取担当者が注文を「受渡済み」にする。
8. レジで注文をキャンセルした場合は、受取用PCにも取消を即時表示する。

本機能は、1日1,000〜2,000会計、最混雑時に全レジ合計で1分5〜6会計程度の利用を想定する。

---

## 2. システム構成

### 2.1 端末構成

| 種別 | 台数 | 用途 |
| --- | ---: | --- |
| レジ端末 | 4台 | 商品選択、会計、注文番号表示、販売取消 |
| 受取用PC | 4台 | 対応するレジの注文受信、商品確認、受渡し管理 |
| 管理端末 | 1台以上 | 全体状況、売上、在庫、注文状況、取消履歴の確認 |

### 2.2 レジと受取場所の対応

レジと受取場所は完全に1対1で固定する。

| レジ | 受取場所 | 色紙の初期設定 |
| ---: | ---: | --- |
| レジ1 | 受取1 | 赤 |
| レジ2 | 受取2 | 青 |
| レジ3 | 受取3 | 緑 |
| レジ4 | 受取4 | 水色 |

色紙の色は運用設定として変更可能にしてもよいが、システム上の振り分けは色ではなく `register_id` と `station_id` を使用する。

### 2.3 データと通信の役割

| 構成要素 | 役割 |
| --- | --- |
| Cloudflare D1 | 売上、商品明細、在庫、注文番号、受渡し状態の正式記録 |
| リアルタイム通知基盤 | 新規注文、取消、状態変更を接続中の受取用PCへ即時通知 |
| WebSocket | 受取用PCへのリアルタイム配信 |
| 定期同期API | 接続切れや通知取りこぼしに備え、D1の正式記録と画面を再同期 |

リアルタイム通知は画面更新のために使用し、注文記録の正しさはD1を基準とする。

---

## 3. ロールと認証

### 3.1 ロール

| ロール | 主な権限 |
| --- | --- |
| `staff` | レジ選択、会計、現在のレジの販売履歴確認、販売取消 |
| `pickup` | 自分の受取場所の注文閲覧、受渡済み、未受渡しへの復元、取消確認 |
| `admin` | 全レジ・全受取場所の閲覧、販売取消、在庫、履歴、集計 |
| `owner` | `admin` の全機能、商品・設定・アカウント管理 |

### 3.2 レジ側アカウント

レジ4台は共通の `staff` アカウントを使用する。

ログイン後、各端末で使用するレジを選択する。

```text
使用するレジを選択してください

[ レジ1 ]  [ レジ2 ]  [ レジ3 ]  [ レジ4 ]
```

選択したレジ番号は署名セッションに保存する。

```json
{
  "role": "staff",
  "username": "staff",
  "registerId": 2,
  "exp": 0
}
```

会計APIおよび取消APIは、リクエスト本文のレジ番号ではなく、署名済みセッションの `registerId` を使用する。

### 3.3 受取側アカウント

受取用PCには4つの個別アカウントを用意する。

```text
pickup-1
pickup-2
pickup-3
pickup-4
```

セッションには受取場所番号を保存する。

```json
{
  "role": "pickup",
  "username": "pickup-2",
  "stationId": 2,
  "exp": 0
}
```

受取用APIは、URLやリクエスト本文で指定された番号ではなく、セッションの `stationId` を使用して注文を絞り込む。

### 3.4 受取用資格情報

既存の `settings` 方式を維持する場合、次の設定キーを追加する。

```text
pickup_1_username
pickup_1_password_hash
pickup_2_username
pickup_2_password_hash
pickup_3_username
pickup_3_password_hash
pickup_4_username
pickup_4_password_hash
```

パスワードは平文保存しない。

---

## 4. 注文番号仕様

### 4.1 表示形式

注文番号は、ランダムな英数字4文字とする。

```text
7KQ2
M4RK
P9AX
2TX4
```

連番は使用しない。

### 4.2 使用文字

手書きや読み上げで誤認しやすい文字を除外する。

使用候補文字:

```text
23479ACDEFHJKMNPQRTUVWXYZ
```

除外する主な文字:

```text
0 O
1 I L
5 S
8 B
```

### 4.3 発行方法

注文番号はサーバー側で、暗号学的に安全な乱数生成手段を用いて発行する。

1. 4文字のコードを生成する。
2. 同じ営業日に使用済みでないか確認する。
3. 未使用なら注文へ割り当てる。
4. 重複していた場合は再生成する。
5. 一意制約違反が発生した場合も再生成する。

最大10回まで再試行し、それでも発行できない場合は会計を完了せず、注文番号発行エラーとして扱う。

### 4.4 一意性

注文番号は、同じ営業日では4つのレジ全体で重複させない。

```text
UNIQUE(business_date, pickup_code)
```

色紙を取り違えた場合でも、注文番号だけで注文を特定できるようにする。

### 4.5 営業日

`business_date` は、設定されたタイムゾーンにおける日付を使用する。既定は `Asia/Tokyo` とする。

日付が変われば、過去に使った注文番号を再利用できる。

### 4.6 表示規則

- 常に大文字で表示する。
- 等幅または識別しやすい太字フォントを使用する。
- レジ完了画面では、注文番号を画面内で最も大きく表示する。
- 自動では消さず、担当者が「次の会計へ」を押すまで保持する。
- 注文番号の横に、使用中のレジ番号と色紙の色を表示する。

---

## 5. 画面仕様

## 5.1 レジ選択画面

### URL

```text
/staff/register/select
```

### 表示項目

- ログイン中のユーザー名
- レジ1〜4の選択ボタン
- 各レジに対応する色紙の色
- 現在選択中のレジ

### 動作

- レジを選択すると署名セッションを更新する。
- 選択後は `/staff/register` へ遷移する。
- レジ変更時は確認ダイアログを表示する。
- 会計処理中または未確定の商品がある場合はレジ変更できない。

---

## 5.2 レジ画面

### URL

```text
/staff/register
```

既存のレジ機能に、次の表示を追加する。

- 現在のレジ番号
- 対応する受取場所番号
- 色紙の色
- 接続状態
- 最近の会計への導線

画面上部の例:

```text
レジ2　→　受取2　　色紙：青
```

### 会計確定ボタン

会計確定中は二重操作を防ぐためボタンを無効化し、処理中表示を出す。

```text
会計を確定しています…
```

---

## 5.3 会計完了画面

会計成功時、次を表示する。

```text
┌──────────────────────┐
│        レジ2          │
│                      │
│         M4RK         │
│                      │
│   青い色紙に記入      │
│                      │
│ 合計 1,000円          │
│ お釣り 200円          │
│                      │
│ [キャンセル]          │
│ [次の会計へ]          │
└──────────────────────┘
```

### 表示項目

- レジ番号
- 注文番号
- 色紙の色
- 商品内容
- 合計金額
- 預かり金額
- 釣銭
- 販売ID
- キャンセルボタン
- 次の会計へ進むボタン

### 動作

- 「次の会計へ」を押すまで注文番号を保持する。
- ブラウザ再読み込み後も、直前の会計結果を再取得できるようにする。
- 同じ冪等キーによる再送では、同じ販売IDと注文番号を返す。

---

## 5.4 最近の会計画面

### URL

```text
/staff/register/recent-sales
```

現在選択中のレジで行われた最近の会計を表示する。

### 表示項目

- 注文番号
- 会計時刻
- 商品一覧
- 合計金額
- 受渡し状態
- 販売状態
- キャンセルボタン

既定表示件数は20件とする。

`staff` は、現在選択中のレジの会計だけを閲覧・取消できる。

---

## 5.5 受取画面

### URL

```text
/pickup
```

ログイン中の受取アカウントに対応する注文だけを表示する。

画面例:

```text
受取窓口2                           ● 接続中
未受渡し 4件

┌──────────────────────────┐
│ M4RK                        1分前 │
│                                │
│ 鮭おにぎり ×2                  │
│ 唐揚げ       ×1                │
│ お茶         ×1                │
│                                │
│        [受渡済みにする]         │
└──────────────────────────┘
```

### 表示順

未受渡し注文は、古い注文を上、新しい注文を下に表示する。

### 新規注文時の動作

- 注文カードを追加する。
- 未受渡し件数を更新する。
- 短い通知音を鳴らす。
- 新規カードを数秒間強調する。
- 注文番号を大きく表示する。
- 同じ注文IDを重複受信した場合は二重表示しない。

### 注文カードの表示項目

- 注文番号
- 注文時刻
- 経過時間
- 商品名
- 数量
- 合計商品点数
- 受渡済みにするボタン

金額情報は受取担当には原則表示しない。

---

## 5.6 受渡済み一覧

受渡済みにした注文は未受渡し一覧から外し、画面下部の「直近の受渡済み」に移動する。

```text
直近の受渡済み

M4RK　1分前　[未受渡しへ戻す]
7KQ2　3分前　[未受渡しへ戻す]
```

既定では直近10件を表示する。

押し間違いに備え、受渡済み注文を未受渡しへ戻せるようにする。

---

## 5.7 キャンセル表示

レジで販売がキャンセルされた場合、受取画面から注文を即座に消してはならない。

次のような取消カードを表示する。

```text
┌──────────────────────────┐
│ 注文キャンセル                 │
│                                │
│ M4RK                           │
│                                │
│ 鮭おにぎり ×2                  │
│ 唐揚げ       ×1                │
│                                │
│ レジ側で取り消されました       │
│                                │
│      [取消を確認しました]       │
└──────────────────────────┘
```

受取担当者が「取消を確認しました」を押すまで表示を残す。

---

## 5.8 管理画面

管理画面に、全レジ・全受取場所の状況を確認する画面を追加する。

### URL

```text
/admin/fulfillment
```

### 表示項目

- レジ別の未受渡し件数
- 受取場所別の未受渡し件数
- 最も古い未受渡し注文の経過時間
- 新規注文
- 受渡済み注文
- キャンセル注文
- 取消未確認件数
- リアルタイム接続中の受取PC

管理者は全注文を検索、閲覧、取消できる。

---

## 6. 注文状態

### 6.1 受渡し状態

通常の運用状態は次の2段階とする。

| 状態 | 値 | 意味 |
| --- | --- | --- |
| 未受渡し | `pending` | 会計済みで、まだ商品を渡していない |
| 受渡済み | `delivered` | 来場者へ商品を渡した |

例外状態として、次を追加する。

| 状態 | 値 | 意味 |
| --- | --- | --- |
| キャンセル | `canceled` | レジ側で販売が取り消された |

### 6.2 状態遷移

```text
pending ──受渡済み──→ delivered
   ↑                       │
   └────未受渡しへ戻す────┘

pending   ──キャンセル──→ canceled
delivered ──キャンセル──→ canceled
```

`canceled` から他の状態へは戻さない。

取消確認は状態とは別に `cancel_acknowledged_at` で管理する。

---

## 7. 会計処理

### 7.1 処理順序

レジ担当が「会計確定」を押した場合、サーバーは次を行う。

1. セッションの認証と `registerId` を確認する。
2. `sales_open` が有効であることを確認する。
3. リクエストの冪等キーを確認する。
4. 商品の存在、販売可否、価格、在庫を確認する。
5. 支払方法、預かり金額、釣銭を確認する。
6. 注文番号を生成する。
7. `sales` を作成する。
8. `sale_items` を作成する。
9. 在庫を減算する。
10. `stock_events` を作成する。
11. `fulfillment_orders` を作成する。
12. D1への保存成功後、レジ画面へ販売結果を返す。
13. 対応する受取場所へ `order.created` を通知する。

D1への保存に失敗した場合、注文番号をレジ画面に表示してはならない。

### 7.2 会計成功レスポンス

```json
{
  "ok": true,
  "data": {
    "saleId": "sale_uuid",
    "idempotencyKey": "checkout_uuid",
    "registerId": 2,
    "stationId": 2,
    "pickupCode": "M4RK",
    "totalAmount": 1000,
    "paidAmount": 1200,
    "changeAmount": 200,
    "fulfillmentStatus": "pending",
    "createdAt": "2026-07-16T12:34:56+09:00"
  }
}
```

### 7.3 冪等性

同じ冪等キーで再送された場合、新しい販売や注文番号を作成しない。

既に成功している販売結果を取得し、同じ `saleId` と `pickupCode` を返す。

---

## 8. キャンセル仕様

### 8.1 権限

`staff`、`admin`、`owner` が販売をキャンセルできる。

`staff` は現在選択中のレジで作成された販売だけをキャンセルできる。

`admin` と `owner` は全レジの販売をキャンセルできる。

### 8.2 キャンセル確認

キャンセル前に、注文番号、商品、金額、受渡し状態を表示する。

```text
注文 M4RK をキャンセルしますか？

商品在庫、売上、受取画面に影響します。

[戻る] [キャンセルを確定]
```

### 8.3 未受渡し注文のキャンセル

`pending` の注文をキャンセルした場合は、次をまとめて実行する。

- `sales.status = canceled`
- 販売数量を在庫へ戻す
- `stock_events` に取消イベントを追加する
- `fulfillment_orders.status = canceled`
- `canceled_at` を保存する
- 受取用PCへ `order.canceled` を通知する

### 8.4 受渡済み注文のキャンセル

受渡済み注文でも `staff` がキャンセルできる。

ただし、次の強い警告を表示する。

```text
この注文は既に受渡済みです。

商品を客へ渡した後に在庫を戻すと、
システム上の在庫と実在庫が一致しない可能性があります。
```

キャンセル時に次を選択させる。

```text
[ 商品が返却されたため在庫へ戻す ]
[ 商品は返却されていないため在庫へ戻さない ]
```

- 商品が返却された場合は在庫を戻す。
- 商品が返却されていない場合は在庫を戻さず、売上と受渡し注文だけをキャンセルする。
- どちらを選択したかを監査履歴に保存する。

### 8.5 二重キャンセル防止

既に `canceled` の販売に対する再キャンセルはHTTP 409で拒否する。

### 8.6 受取側の取消確認

受取担当者が取消カードの「取消を確認しました」を押したら、`cancel_acknowledged_at` を保存する。

確認後は通常一覧から外すが、管理画面と履歴には残す。

---

## 9. リアルタイム通信仕様

### 9.1 推奨構成

リアルタイム通知には、Cloudflare Worker、Durable Objects、WebSocketを使用する。

```text
Cloudflare Pages
  React / Pages Functions / Hono

Cloudflare Worker
  Durable Objectによる接続管理

Cloudflare D1
  正式な注文・売上・在庫記録
```

### 9.2 接続単位

受取用PCはログイン後、自分の `stationId` でWebSocketへ接続する。

```text
station-1
station-2
station-3
station-4
admin-all
```

レジ2で作成された注文は `station-2` と `admin-all` にのみ配信する。

### 9.3 イベント共通形式

```json
{
  "eventId": "event_uuid",
  "type": "order.created",
  "stationId": 2,
  "occurredAt": "2026-07-16T12:34:56+09:00",
  "data": {}
}
```

`eventId` を用いて、クライアント側で重複イベントを除去する。

### 9.4 新規注文イベント

```json
{
  "eventId": "event_uuid",
  "type": "order.created",
  "stationId": 2,
  "occurredAt": "2026-07-16T12:34:56+09:00",
  "data": {
    "saleId": "sale_uuid",
    "pickupCode": "M4RK",
    "registerId": 2,
    "status": "pending",
    "items": [
      {
        "productId": "onigiri_salmon",
        "productName": "鮭おにぎり",
        "quantity": 2
      }
    ],
    "createdAt": "2026-07-16T12:34:56+09:00"
  }
}
```

### 9.5 状態変更イベント

```text
order.delivered
order.restored
order.canceled
order.cancel_acknowledged
```

### 9.6 通信切断時

受取画面は接続状態を常時表示する。

```text
● 接続中
● 再接続中
● オフライン
```

WebSocketが切断された場合は指数バックオフで自動再接続する。

再接続直後にD1から未受渡し注文と未確認キャンセルを再取得する。

### 9.7 定期同期

通知取りこぼしに備え、受取画面は60秒ごとにD1の注文一覧と同期する。

次の場合にも同期する。

- 初回表示時
- WebSocket接続成功時
- WebSocket再接続時
- ブラウザタブが再び表示状態になった時
- ユーザーが「再同期」を押した時

常時の短間隔ポーリングは行わない。

---

## 10. API仕様

| Method | Path | 認証 | 概要 |
| --- | --- | --- | --- |
| POST | `/api/staff/register/select` | staff | 使用するレジを選択 |
| GET | `/api/staff/register/current` | staff | 現在選択中のレジを取得 |
| POST | `/api/staff/register/checkout` | staff/admin/owner | 会計、注文番号発行、受取注文作成 |
| GET | `/api/staff/register/recent-sales` | staff/admin/owner | 最近の会計を取得 |
| POST | `/api/sales/:saleId/cancel` | staff/admin/owner | 販売取消、在庫処理、受取通知 |
| GET | `/api/pickup/orders` | pickup/admin/owner | 未受渡し、直近受渡済み、取消未確認を取得 |
| GET | `/api/pickup/live` | pickup/admin/owner | WebSocket接続 |
| POST | `/api/pickup/orders/:saleId/deliver` | pickup/admin/owner | 受渡済みに変更 |
| POST | `/api/pickup/orders/:saleId/restore` | pickup/admin/owner | 未受渡しへ戻す |
| POST | `/api/pickup/orders/:saleId/ack-cancel` | pickup/admin/owner | 取消確認 |
| GET | `/api/admin/fulfillment/orders` | admin/owner | 全受取注文の検索・閲覧 |
| GET | `/api/admin/fulfillment/summary` | admin/owner | レジ別・受取場所別集計 |

### 10.1 レジ選択リクエスト

```json
{
  "registerId": 2
}
```

`registerId` は1〜4のみ許可する。

### 10.2 受渡済みリクエスト

```json
{
  "expectedStatus": "pending"
}
```

状態競合を防ぐため、現在状態が `pending` の場合だけ更新する。

### 10.3 キャンセルリクエスト

```json
{
  "reason": "注文内容の間違い",
  "restoreStock": true
}
```

- `pending` の場合、`restoreStock` は常に `true` とする。
- `delivered` の場合、利用者が在庫返却の有無を選択する。

---

## 11. データベース変更

## 11.1 `sales` への追加

| カラム | 型 | 内容 |
| --- | --- | --- |
| `register_id` | INTEGER | 会計を行ったレジ番号。1〜4 |
| `cancel_reason` | TEXT | 取消理由 |
| `cancel_restore_stock` | INTEGER | 取消時に在庫を戻したか |
| `canceled_by_username` | TEXT | 取消を実行したユーザー |

### 制約

```text
register_id BETWEEN 1 AND 4
```

---

## 11.2 `fulfillment_orders`

```sql
CREATE TABLE fulfillment_orders (
  id TEXT PRIMARY KEY,
  sale_id TEXT NOT NULL UNIQUE,
  business_date TEXT NOT NULL,
  register_id INTEGER NOT NULL,
  station_id INTEGER NOT NULL,
  pickup_code TEXT NOT NULL,
  status TEXT NOT NULL DEFAULT 'pending',
  delivered_at TEXT,
  delivered_by_username TEXT,
  canceled_at TEXT,
  cancel_acknowledged_at TEXT,
  cancel_acknowledged_by_username TEXT,
  created_at TEXT NOT NULL,
  updated_at TEXT NOT NULL,

  UNIQUE (business_date, pickup_code),
  CHECK (register_id BETWEEN 1 AND 4),
  CHECK (station_id BETWEEN 1 AND 4),
  CHECK (register_id = station_id),
  CHECK (status IN ('pending', 'delivered', 'canceled')),
  FOREIGN KEY (sale_id) REFERENCES sales(id)
);
```

商品内容は既存の `sale_items` と `products` を参照して取得する。

---

## 11.3 `fulfillment_events`

受渡し操作の監査履歴を保存する。

```sql
CREATE TABLE fulfillment_events (
  id TEXT PRIMARY KEY,
  fulfillment_order_id TEXT NOT NULL,
  event_type TEXT NOT NULL,
  from_status TEXT,
  to_status TEXT,
  actor_role TEXT NOT NULL,
  actor_username TEXT NOT NULL,
  reason TEXT,
  metadata_json TEXT,
  created_at TEXT NOT NULL,

  CHECK (
    event_type IN (
      'created',
      'delivered',
      'restored',
      'canceled',
      'cancel_acknowledged'
    )
  ),
  FOREIGN KEY (fulfillment_order_id) REFERENCES fulfillment_orders(id)
);
```

---

## 11.4 推奨インデックス

```sql
CREATE INDEX idx_fulfillment_station_status_created
ON fulfillment_orders(station_id, status, created_at);

CREATE INDEX idx_fulfillment_business_code
ON fulfillment_orders(business_date, pickup_code);

CREATE INDEX idx_sales_register_created
ON sales(register_id, created_at);

CREATE INDEX idx_fulfillment_events_order_created
ON fulfillment_events(fulfillment_order_id, created_at);
```

---

## 12. 整合性と競合対策

### 12.1 注文番号の競合

- DBの一意制約を最終的な保証とする。
- 発行前確認だけに依存しない。
- 一意制約違反時は別番号を生成して再試行する。

### 12.2 会計の二重送信

- 既存の `idempotency_key` を利用する。
- 同じキーで複数の注文番号を発行しない。

### 12.3 在庫競合

- 在庫確認と在庫減算を一連のDB処理として実行する。
- 在庫不足なら販売、受取注文、在庫イベントを作成しない。
- 在庫を0未満にしない。

### 12.4 受渡し操作の競合

- 更新時に現在状態を条件へ含める。
- 既に別端末で状態変更されていた場合はHTTP 409を返し、最新状態を再取得する。

### 12.5 通知失敗

D1への保存が成功し、WebSocket通知だけが失敗した場合でも、会計は成功扱いとする。

受取用PCは再接続または定期同期によって注文を取得する。

---

## 13. エラー仕様

| HTTP | コード | 内容 |
| ---: | --- | --- |
| 400 | `INVALID_REGISTER_ID` | レジ番号が1〜4ではない |
| 400 | `INVALID_PAYMENT` | 支払条件が不正 |
| 401 | `UNAUTHORIZED` | 未ログインまたはセッション不正 |
| 403 | `FORBIDDEN_STATION` | 他の受取場所へアクセスしようとした |
| 404 | `SALE_NOT_FOUND` | 販売が存在しない |
| 404 | `ORDER_NOT_FOUND` | 受取注文が存在しない |
| 409 | `INSUFFICIENT_STOCK` | 在庫不足 |
| 409 | `ORDER_CODE_CONFLICT` | 注文番号発行に失敗 |
| 409 | `ALREADY_CANCELED` | 既にキャンセル済み |
| 409 | `STATUS_CONFLICT` | 受渡し状態が他端末で変更済み |
| 409 | `SALES_CLOSED` | 販売停止中 |
| 500 | `REALTIME_NOTIFICATION_FAILED` | 通知に失敗。ただし注文保存済みの場合あり |

画面には技術的なコードではなく、担当者が次の行動を判断できる日本語メッセージを表示する。

---

## 14. 非機能要件

### 14.1 想定負荷

| 項目 | 想定値 |
| --- | ---: |
| 1日の会計数 | 1,000〜2,000件 |
| 最大会計速度 | 全レジ合計で1分5〜6件 |
| レジ端末 | 4台 |
| 受取用PC | 4台 |
| 管理端末 | 1台以上 |

### 14.2 応答目標

- 会計確定APIは通常2秒以内に結果を返す。
- 会計成功後、対応する受取画面へ通常2秒以内に注文を表示する。
- 通知失敗時も、定期同期により最大60秒以内に表示を復元する。
- 受渡済み操作は通常2秒以内に反映する。

### 14.3 可用性

- 一時的なWebSocket切断から自動復旧する。
- 受取用PCを再起動しても未受渡し注文を復元する。
- ブラウザを再読み込みしても注文状態を失わない。
- リアルタイム通知サービス停止時も、D1に保存された注文を定期同期で取得できる。

### 14.4 無料枠を意識した通信

- 1秒単位の常時ポーリングは行わない。
- 通常はWebSocketで通知する。
- 60秒ごとの安全同期を行う。
- 画面が非表示の間は不要な描画更新を抑える。
- Cloudflareの利用量を本番前の負荷試験で確認する。

---

## 15. セキュリティ要件

- セッションは署名付きHttpOnly Cookieを使用する。
- 本番では `SESSION_SECRET` 未設定時に起動を拒否する。
- 受取アカウントは、自分の `stationId` の注文だけを取得できる。
- `staff` は現在選択中の `registerId` の販売だけを取消できる。
- 商品価格、レジ番号、受取場所番号、注文番号をクライアント入力だけで信用しない。
- 取消、受渡済み、未受渡しへの復元、取消確認を監査履歴に残す。
- WebSocket接続時にもセッション認証を行う。

---

## 16. 運用手順

## 16.1 開店前

1. レジ4台と受取用PC4台を起動する。
2. レジ端末でstaffログインを行う。
3. 各端末で正しいレジ番号を選択する。
4. 受取用PCで `pickup-1`〜`pickup-4` にログインする。
5. レジと受取場所の対応を確認する。
6. 各受取画面が「接続中」になっていることを確認する。
7. 各レジでテスト会計を行う。
8. 対応する受取用PCだけに注文が表示されることを確認する。
9. 受渡済み、未受渡しへの復元、キャンセル、取消確認を試す。
10. テスト会計を取消し、在庫が元に戻ったことを確認する。
11. `sales_open` を有効にする。

## 16.2 営業中

- 会計完了画面の注文番号を色紙へ正確に記入する。
- 色紙の色とレジ番号を一致させる。
- 受取担当は、番号と商品内容を確認してから渡す。
- 商品を渡した直後に「受渡済みにする」を押す。
- 接続状態が「オフライン」の場合は、再同期を行い、必要に応じて管理者へ連絡する。

## 16.3 閉店時

1. `sales_open` を無効にする。
2. 未受渡し注文が0件であることを確認する。
3. 取消未確認が0件であることを確認する。
4. 売上、在庫、受取履歴をCSVで保存する。
5. 現金とシステム売上を照合する。
6. 最終在庫を確認する。

---

## 17. 障害時対応

### 17.1 受取画面がオフラインになった場合

1. 画面の「再接続」を押す。
2. ブラウザを再読み込みする。
3. 未受渡し一覧がD1から復元されることを確認する。
4. 復旧しない場合は別PCまたは管理画面で該当受取場所を表示する。

### 17.2 レジ会計後に受取画面へ表示されない場合

1. レジ完了画面の注文番号と販売IDを保持する。
2. 受取画面で「再同期」を押す。
3. 管理画面で注文がD1に保存されているか確認する。
4. 保存済みなら受取担当へ口頭で注文内容を伝える。
5. 保存されていなければ会計を再実行せず、管理者が販売履歴を確認する。

### 17.3 インターネット障害

完全オフライン同期は対象外とする。

長時間復旧しない場合に備えて、紙の緊急販売記録表を用意する。

記録項目:

```text
時刻
レジ番号
仮注文番号
商品と数量
合計金額
預かり金額
釣銭
担当者
受渡し状況
```

---

## 18. テスト仕様

### 18.1 単体テスト

- 注文番号に禁止文字が含まれない。
- 同一営業日の注文番号重複時に再生成される。
- 冪等キー再送時に同じ注文番号が返る。
- `registerId` と `stationId` が常に一致する。
- 他レジの注文をstaffが取消できない。
- 他受取場所の注文をpickupが取得できない。
- `pending` から `delivered` へ変更できる。
- `delivered` から `pending` へ復元できる。
- `canceled` から状態変更できない。
- 二重キャンセルで在庫が二重に戻らない。

### 18.2 結合テスト

- レジ1の会計が受取1だけに表示される。
- レジ2〜4でも同様に正しく振り分けられる。
- WebSocket切断中の注文が再接続時に復元される。
- 受渡済み操作が別端末にも反映される。
- キャンセルが受取画面に赤い取消カードとして表示される。
- 取消確認後も管理履歴に残る。
- 受渡済み注文のキャンセル時に在庫返却の有無が正しく反映される。

### 18.3 負荷テスト

最低限、次の条件で事前試験を行う。

- レジ4台相当から同時に会計する。
- 1分6会計を30分継続する。
- 2,000件の注文データを投入する。
- 受取用PC4台相当のWebSocketを接続する。
- 注文番号の重複、通知遅延、在庫不整合、APIエラーを確認する。

---

## 19. 受入条件

次をすべて満たした場合、本機能を文化祭本番で利用可能と判断する。

- staffログイン後にレジ1〜4を選択できる。
- 選択中のレジ番号がレジ画面に常時表示される。
- 会計成功時にランダムな4文字の注文番号が発行される。
- 同じ営業日に注文番号が重複しない。
- 会計成功後、対応する受取用PCへ通常2秒以内に注文が表示される。
- レジ1の注文が受取2〜4に表示されない。
- 受取画面に注文番号、商品名、数量、経過時間が表示される。
- 受渡済みと未受渡しへの復元ができる。
- staffが現在のレジの販売をキャンセルできる。
- キャンセル時に受取画面へ取消が表示される。
- 取消は受取担当が確認するまで表示される。
- WebSocket切断後も注文一覧を復元できる。
- 会計の二重送信で販売や注文番号が重複しない。
- 在庫不足時に販売と受取注文が作成されない。
- 1日2,000会計、最大1分6会計の試験で安定動作する。
- 型チェック、単体テスト、ビルド、主要E2Eテストが成功する。

---

## 20. 実装優先順位

### 優先度S

1. `sales.register_id` の追加
2. `fulfillment_orders` の追加
3. ランダム注文番号発行と一意制約
4. 会計処理への受取注文作成の統合
5. 受取画面の未受渡し一覧
6. レジと受取場所の1対1認可
7. staffによる販売取消
8. キャンセルの受取側表示
9. 再接続時のD1同期

### 優先度A

1. WebSocketによる即時通知
2. 受渡済みと未受渡しへの復元
3. 取消確認
4. 管理画面の受取状況一覧
5. 接続状態表示と再同期ボタン
6. 通知音

### 優先度B

1. 待ち時間の強調表示
2. レジ別・受取場所別集計
3. 受取履歴CSV
4. 色紙設定の管理画面化
5. 混雑状況の可視化
